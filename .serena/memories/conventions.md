Core owns IDs, ordering, section/type/targets/answers/source_text and all fixed GeneratedDocument fields; composer may change only question text. Provider metadata stays out of public wire JSON. Preserve old JSON deserialization/defaults and public parser APIs. Validate unknown/duplicate/missing IDs and sentinel/blank invariants explicitly; never silently filter tasks. Separate transport retry from content retry; fallback is explicit and task-scoped. Keep provider-specific envelopes/errors inside adapters, exposing common Core types.