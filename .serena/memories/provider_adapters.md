Phase 3 provider boundary: `src/http.rs` owns bounded blocking reqwest retries and redacted transport errors. Gemini/OpenAI-compatible adapters implement `QuestionComposer`; provider envelopes must be converted through `parse_compose_output`. Planner only content-retries `InvalidResponse`/`EmptyResponse`; transport/provider errors are terminal. Structured mode support and local URL candidates live in provider modules.