Phase 5 observability: `observability` defines body-free JSONL events, FNV-1a hashes, process-unique RunContext, Noop/JSON stderr sinks and MetricsSummary. Existing public generation entry remains no-op; CLI uses observed entry only under --verbose or FLOWCLOZE_LOG=debug. No metadata in GeneratedDocument, sidecars, parallel batches, or automatic policy tuning. `max_concurrent_batches` remains validated/observed but execution is sequential.