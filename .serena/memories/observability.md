Phase 5 observability: `observability` defines body-free JSONL events, FNV-1a hashes, process-unique RunContext, Noop/JSON stderr sinks and MetricsSummary. Human-readable progress is independent in `progress`; legacy public entrypoints delegate to Noop sinks, while CLI always writes plain progress and adds JSONL under `--verbose` or `FLOWCLOZE_LOG=debug`. Private planner execution errors retain actual terminal provider cause for progress classification; public `ComposePlanError`/`FailureReason` shapes stay compatible. No metadata in GeneratedDocument, sidecars, parallel batches, or automatic policy tuning. `max_concurrent_batches` remains validated/observed but execution is sequential. CLI stdout JSON is byte-exact (no appended newline), write_all+flush.
