Common verification/build commands (use RTK wrapper in this environment): `rtk cargo test`, `rtk cargo build --release`, `rtk npm run docs:api:lint`, `rtk npm run docs:api`. CLI smoke: `cargo run -- --help`, `cargo run -- generate --provider local --rewrite never ...`. If Redocly's Node runtime emits the known warning/compatibility failure, rerun the docs command with `NODE_OPTIONS=--no-deprecation`.