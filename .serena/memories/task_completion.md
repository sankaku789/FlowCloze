Task completion baseline: `rtk cargo test`; `rtk cargo build --release`; `rtk npm run docs:api:lint`; `rtk npm run docs:api`. Add focused mock/compatibility tests for changed generation behavior; real-provider live smoke is optional and should be separately enabled. For Redocly Node warning/compatibility issues, use `NODE_OPTIONS=--no-deprecation` with the docs command. Do not treat live API access as required for completion.