Final-review generation invariants: fallback=draft never changes the initial batch plan; retain planner-confirmed tasks and draft only exhausted content failures, while transport failures can draft their affected batch. Unknown response IDs are fatal/no-retry but known valid items may be confirmed before return. Sentinel validation treats source-existing complete FC tokens as baseline; new complete foreign-namespace tokens are errors. Adapter event prompt_hash hashes `build_compose_request_prompt`'s final adapter input; identity has no prompt hash. Local candidate composers must retain one adapter per URL so Auto structured-output capability state survives batches/retries. Config explicit `FLOWCLOZE_CONFIG` missing files are errors; implicit cwd config absence is optional.