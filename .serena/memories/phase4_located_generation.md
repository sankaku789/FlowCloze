Phase 4 located path is public `generate_markdown_with_composer(markdown, GenerateMarkdownOptions, QuestionComposer)`. It privately parses raw qblock spans, builds document-scoped FNV sentinel tokens, and normalizes validated sentinel output to `＿＿＿`; legacy parser/scaffold remain compatibility paths. Do not put provider metadata into `GeneratedDocument`; typed validation is `validation::validate_generated_documents`, JSON APIs are wrappers.